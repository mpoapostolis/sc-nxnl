package com.skincoach.app.ui.theme

import androidx.compose.ui.graphics.Color

// Skin Coach palette — soft, warm, pretty. A blush-and-rose skincare aesthetic.
val Paper = Color(0xFFFBF2EF)          // soft blush-cream background
val Sand = Color(0xFFF3E3DE)           // deeper blush block
val Cloud = Color(0xFFFFFBFA)          // warm near-white card surface

val Ink = Color(0xFF2A2329)            // warm near-black, primary text
val InkSoft = Color(0xFF796B71)        // soft mauve-grey, secondary text
val InkFaint = Color(0xFFB5A7AC)       // faint mauve, hints

// Primary accent — kept the name "Terracotta" for stability; the hue is a warm rose-coral.
val Terracotta = Color(0xFFD2616C)     // warm rose-coral accent
val TerracottaDeep = Color(0xFFB84A5A) // deeper rose — pressed
val TerracottaSoft = Color(0xFFF6DDDD) // soft blush-pink tint

val Sage = Color(0xFF7CA98D)           // soft fresh sage — positive / progress
val SageSoft = Color(0xFFE3ECE4)

val Line = Color(0xFFEEDED9)           // soft blush hairline
val ErrorRed = Color(0xFFC5666C)       // soft dusty rose — low scores
